pythonRestApi:
  config: |
    server:
      host: "0.0.0.0"
      port: 8080
      cowsay: "Something different!!"
    redis:
      enabled: True
      host: "redis-master.default.svc.cluster.local"
      port: "6379"
